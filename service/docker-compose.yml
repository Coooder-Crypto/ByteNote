version: "3.9"

services:
  collab:
    build: .
    container_name: bytenote-collab
    restart: unless-stopped
    environment:
      WS_PORT: 1234
      # 允许的前端来源，留空为不限制。例如 http://localhost:3000
      ALLOWED_ORIGINS: "http://localhost:3000"
    ports:
      - "1234:1234"
